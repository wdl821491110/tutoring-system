# 2026-06-12 — WeUI 美化任务 · 首页样板

## 全局色板简化
- `c-bg`: F5F6FA → #F5F5F5（更接近微信原生灰）
- `c-text`: #1A1A1A → #000000（纯黑，微信原生）
- `c-text-secondary`: #8E8E93 → #888888
- `c-text-hint`: #C7C7CC → #B2B2B2
- `c-border`: #F0F0F0 → #E5E5E5
- 删除 `c-stat-1` ~ `c-stat-5` 五组配色变量
- 所有按钮改为实色 `var(--c-primary)`（去除渐变），仅 progress-bar 保留渐变

## 全部页面 WeUI 改造（7/7）

### student
- mp-searchbar 替换自定义搜索栏
- mp-cells + mp-slideview 左滑编辑/删除
- mp-half-screen-dialog 替代表单弹窗
- 头像去渐变→纯色 (#10AEFF 男 / #FF7A9E 女)
- 新建 students.wxss

### schedules
- mp-cells + mp-cell 重构排课列表（左色条状态指示 + slot 内自定义操作按钮）
- mp-half-screen-dialog 替换新增排课弹窗
- 保留自定义日期/状态筛选栏（WeUI 无等价组件）
- 新建 schedules.wxss

### records
- mp-cells 重构记录列表
- 两个 mp-half-screen-dialog（新增记录 + 备注查看）
- 保留自定义日期筛选栏和分页器
- 新建 records.wxss

### profile
- 用户信息区→独立卡片；账号/权限→mp-cells+mp-cell
- 简化 profile.wxss

### login
- emoji 图标全替换：📚→"课"字方块，👤/🔒→mp-icon(info/warn)
- Logo渐变→实色绿，btn渐变→实色绿
- 更新 login.wxss

### privacy
- 所有内联 style 提取为 CSS 类
- 补全 privacy.wxss

## 首页重写（pages/index/）
- **index.json**: 注册 mp-icon、mp-cells、mp-cell
- **index.wxml**: 数据概览用 mp-cells+mp-cell；今日课程用 mp-cells+mp-cell；排行因含进度条用自定义 rank-cell（模仿 weui-cell 样式）
- **index.wxss**: 新建 120 行，仅含排名区域 cell 仿 WeUI 样式 + 空状态卡片 + 骨架屏
- 消除全部内联 style，emoji 仅保留空状态插画 📅

## 方向确认
- 用户选：深度 WeUI 集成 + mp-half-screen-dialog 替代表单弹窗 + WeUI 内置图标 + 极简白卡片 + 强化视觉 + 先做样板
- 第二轮要求：最高限度逼近微信原生态，删除多余配色，干净清爽

## 第三轮修复（截屏反馈）
- mp-cell value 换行：给 `.weui-cell__ft` / `.__ft_in-access` 加 `white-space:nowrap;flex-shrink:0`
- mp-cell title 过长：给 `.weui-cell__bd` 加 `overflow:hidden;text-overflow:ellipsis;white-space:nowrap;flex:1`
- icon 不渲染：去掉第一个 mp-cell 的 `icon="info"`
- cell flex 显示：`.weui-cell` 显式 `display:flex !important`

## 第四轮 — 架构收敛 + 表单体验 + 动效清理

### 提取 form-sheet 通用组件 (components/form-sheet/)
- 封装 mp-half-screen-dialog 弹窗壳，通过 slot 让各页面置入自定义表单字段
- 支持 confirmText / confirmDisabled properties
- 禁止态按钮文案自动切换"请完善信息"
- 三个页面（students/schedules/records）全部接入

### 表单实时校验（3 个页面）
- blur 失焦校验：红色边框（form-row--error）+ 底部错误文字
- 必填项未填 → confirmDisabled=true → 按钮置灰
- 输入/picker选择时自动清除对应字段错误

### 全局排版体系
新增 CSS 变量：--font-xs(12)~--font-xxl(24)、--fw-normal~black、--lh-tight(1.3)/normal(1.5)/loose(1.8)

### 动效收敛微信原生
- 删除 .slide-up / .stagger-item
- .card--hover:active → 仅 background:#F5F5F5
- .fade-in → 200ms
- 新增 .skeleton-out + .content-in 交叉淡入淡出

## 第五轮 — 全局去配色、布局原生

### 页面容器
- .page-container 去横向padding，cells贴边

### WeUI cells 微信原生化
- .weui-cells: 删box-shadow、border-radius:0、margin-bottom:8px
- .weui-cells__title: 13px/400
- .weui-cell__bd: 17px（微信标准）
- .weui-cell__ft: 15px
- 分割线 #E5E5E5 hairline

### 配色删除
- 学生头像统一微信绿
- 排课列表去左色条
- 状态标签只剩绿/灰
- 首页统计去link箭头
- 排行进度条绿色+非首位opacity:0.4
- 所有卡片去阴影/圆角
- 登录页logo/卡片/按钮去阴影
- 个人中心头像区去阴影/圆角
