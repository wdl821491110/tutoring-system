# 2026-06-07 工作记录

## 项目审查 + 全部修复
- 完成"课消管理系统"小程序全面审查并修复 P0/P1/P2 问题
- 审查报告: `.workbuddy/review/2026-06-07-review.md`

### 修复清单
1. **补全缺失文件**: login.json/wxss, profile.json/wxss, privacy 页面全套
2. **模块化架构**: 从 app.js 拆分出 utils/request.js (统一请求) + utils/auth.js (认证)
3. **隐私合规**: 新增 privacy 页面 + app.js 隐私授权预检 + 登录页/个人中心隐私入口
4. **API 并行化**: schedules.js 和 records.js 的嵌套调用改为 Promise.all + 错误兜底
5. **数据变异修复**: 所有 onFormChange 改用 setData 路径更新 `form.${field}` 而非直接修改 this.data
6. **var → const/let**: 全部 .js 文件改为现代语法
7. **缓存 + loading**: 所有页面加 30s 缓存 + loading 态
8. **全局机制**: onError/onUnhandledRejection/版本更新/网络监听
9. **清理**: 删除 app.wxss.bak、pages$page$page.*、_read_app.json.tmp、未引用图片、自定义 tabbar 组件
10. **app.json**: index 移到首位、统一 #07C160 主题、使用原生 TabBar

## 美化工作 (23:57-00:10)

### 全局设计系统
- 重写 app.wxss (475行): CSS变量色板、阴影层级、圆角规范、骨架屏动画、渐入/上滑动画、胶囊筛选、分页器、学生头像等40+设计类

### 页面美化
| 页面 | 改动 |
|------|------|
| **登录页** | 渐变色Logo、卡片式输入、渐变阴影按钮 |
| **仪表盘** | 5张彩色渐变统计卡、时间轴课程列表、进度条排行、交错动画 |
| **个人中心** | 渐变头像、色标角色标签、图标化权限(✓/✗)、红色边框退出按钮 |
| **学生管理** | 彩色头像卡片(男蓝女粉)、课程标签徽章 |
| **排课管理** | 胶囊筛选器、左侧色条状态卡片、操作按钮组 |
| **课时记录** | 出勤/缺勤双色tag、日期选择卡片化、分页器美化 |

## 二轮美化 (00:18-00:25) — 去 WeUI 化

### 全局新增样式 (app.wxss +120行)
- `.overlay` / `.overlay-mask` / `.overlay-sheet` — 自定义全屏覆盖弹窗（替代 mp-dialog）
- `.overlay-header` / `.overlay-footer` — 弹窗标题栏 + 双按钮底部栏
- `.form-row` / `.form-label` / `.form-input` / `.form-picker-val` / `.form-textarea` — 卡片式表单行（替代 mp-cells/mp-cell）
- `.custom-search` — 圆角搜索栏（替代 mp-searchbar）

### 三页面彻底重写
| 页面 | 移除 | 替换为 |
|------|------|--------|
| **学生管理** | mp-searchbar、mp-dialog、mp-cells/mp-cell | custom-search、overlay-sheet、form-row×7 |
| **排课管理** | mp-dialog、mp-cells/mp-cell | overlay-sheet、form-row×6 |
| **课时记录** | mp-dialog×2、mp-cells/mp-cell | overlay-sheet×2(新增/备注)、form-row×6 |

### JS 事件适配
- `onSearch(e.detail)` → `onSearch(e.detail.value)` 适配 input bindinput
- `saveForm(e.detail.index)` → `closeForm()` + `submitForm()` 按钮独立事件
- `saveSchedule` → `closeForm()` + `submitSchedule()`
- `saveRecord` → `closeForm()` + `submitRecord()`
- `onNoteDialog` → `closeNotes()` + `addNote()`
- 新增 `noop()` 阻止 overlay 滚动穿透
